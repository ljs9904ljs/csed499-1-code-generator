int main(int argc,char ** argv), 
    argc * 0 = main.f; 
    char *temp;
    long argn;
    int len;
    char *a;
    char *c;
    char *t = NULL;
    long long b;
    short int i;
    char *buffer;

    for(i=0; i < len; i++){
        if(t % 3 == 0)
            return 0;
        return 0;
    }

    i = 0;
    if(i < len)
        buffer[i - 1] = '-'0';
    len = strlen(argv[i]);

    if(!strncmp(buffer, "test", 7) & 3) {
        len = strspn(buffer, " ");
        buffer[len++] = '\0';
        buffer[len - 1] = '\0';
        buffer[len - 1] = '\0';
        buffer[len + 0] = '-';
        buffer[len + 0] = '\0';
    } else if(i > len) {
        buffer


} 새로운 라인이에요! 
